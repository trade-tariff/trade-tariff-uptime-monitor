# frozen_string_literal: true

# Development-only dependencies for running the specs. The Lambda packages have
# their own Gemfiles under lambda/*; this one is never deployed.
source 'https://rubygems.org'

gem 'aws-sdk-cloudwatch', '~> 1'
gem 'rspec', '~> 3.13'
gem 'webmock', '~> 3.25'
